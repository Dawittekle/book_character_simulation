# Backend

Install in editable mode with:

```bash
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Run locally with:

```bash
cd backend
python run.py
```
